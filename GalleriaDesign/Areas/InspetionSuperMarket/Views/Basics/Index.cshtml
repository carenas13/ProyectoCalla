@model IEnumerable<Supermarket.Models.Basics>

@{
    ViewBag.Title = "Index";
}

<h2>Basics</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBasics }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBasics }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBasics })
        </td>
    </tr>
}

</table>
