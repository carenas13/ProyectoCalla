@model IEnumerable<Supermarket.Models.SleeveType>

@{
    ViewBag.Title = "Index";
}

<h2>Sleeve Type</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameSleeve)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameSleeve)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSleeveType }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSleeveType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSleeveType })
        </td>
    </tr>
}

</table>
