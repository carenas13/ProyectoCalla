@model IEnumerable<Supermarket.Models.Bouquet_Filler>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bouquetProgram.descriptionProgram)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fillerType.nameFiller)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.checkFiller)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bouquetProgram.descriptionProgram)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fillerType.nameFiller)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.checkFiller)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBouquet }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBouquet }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBouquet })
        </td>
    </tr>
}

</table>
