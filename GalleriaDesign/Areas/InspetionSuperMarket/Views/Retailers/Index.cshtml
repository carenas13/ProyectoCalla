@model IEnumerable<Supermarket.Models.Retailer>

@{
    ViewBag.Title = "Index";
}

<h2>Retailer</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameRetailer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameRetailer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRetailer }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRetailer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRetailer })
        </td>
    </tr>
}

</table>
