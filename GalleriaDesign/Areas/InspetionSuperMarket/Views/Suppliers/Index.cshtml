@model IEnumerable<Supermarket.Models.Supplier>

@{
    ViewBag.Title = "Index";
}

<h2>Suppliers</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameSupplier)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameSupplier)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSupplier }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSupplier }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSupplier })
        </td>
    </tr>
}

</table>
