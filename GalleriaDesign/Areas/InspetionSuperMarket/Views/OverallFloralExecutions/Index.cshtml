@model IEnumerable<Supermarket.Models.OverallFloralExecution>

@{
    ViewBag.Title = "Index";
}

<h2>Overall Store Execution</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameOverall)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameOverall)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idOverallFloral }) |
            @Html.ActionLink("Details", "Details", new { id=item.idOverallFloral }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idOverallFloral })
        </td>
    </tr>
}

</table>
