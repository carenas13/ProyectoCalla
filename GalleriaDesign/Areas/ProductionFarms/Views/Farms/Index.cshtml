@model IEnumerable<ApplicationProductionsFarms.Models.Farms>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/ProductionFarms/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codeFarms)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codeFarms)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idFarms }) |
            @Html.ActionLink("Details", "Details", new { id=item.idFarms }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idFarms })
        </td>
    </tr>
}

</table>
