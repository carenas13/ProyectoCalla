@model IEnumerable<ApplicationProductionsFarms.Models.Program>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codProgram)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codProgram)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProgram }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProgram }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProgram })
        </td>
    </tr>
}

</table>
