@model IEnumerable<ApplicationProductionsFarms.Models.FlowerType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codFlowerType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codFlowerType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCodFlowerType }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCodFlowerType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCodFlowerType })
        </td>
    </tr>
}

</table>
